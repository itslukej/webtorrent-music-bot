# WebTorrent Music Bot

Streams audio from webtorrent in your browser, through a websocket, to a node.js script, to discord. Prototype.

```bash
npx http-server -p 3000
node bot.js # some configuration required
```